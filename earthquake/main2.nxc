
#define turn_right RotateMotorEx(OUT_AB, 60, -310, 100, true, true)
#define forward RotateMotor(OUT_AB, 60, -600)

task main() {
	forward;
	forward;
	turn_right;
	forward;
	turn_right;
	forward;
	forward;
	turn_right;
	forward;	
	turn_right;
}
